apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "plugin-center.fullname" . }}-test-connection"
  labels:
{{ include "plugin-center.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "plugin-center.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
